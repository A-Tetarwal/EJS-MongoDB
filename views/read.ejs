<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EJS and MongoDB</title>
    <link rel="stylesheet" href="/stylesheets/index.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="w-full h-full bg-zinc-900 text-white p-10">
    <a href="/" class="text-blue-500">&leftarrow; Back to Home</a>
    <h1 class="text-3xl tracking-tighter mb-3">All Users</h1>
    <div class="users flex gap-3 flex-wrap">
        <% if(users.length > 0){ %>
            <% users.forEach(user => { %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-72 rounded-lg bg-zinc-700 overflow-hidden">
                        <img src="<%= user.image %>" class="w-full h-full object-cover object-center">
                    </div>
                    <h3 class="text-xl mt-3 tracking-tighter"><%= user.name %></h3>
                    <h5 class="text-zinc-500"><%= user.email %></h5>
                    <div class="flex gap-3 mt-5 justify-between">
                        <a href="/edit/<%= user._id %>" class="text-zinc-100 text-sm">Edit</a>
                        <a href="/delete/<%= user._id %>" class="text-red-500 text-sm">Delete</a>
                    </div>
                </div>
            <% }); %>
        <% } else{ %>
            <h3 class="text-zinc-500">No Users</h3>
        <% } %>

    </div>
    <script src="/javascripts/index.js"></script>
</body>

</html>